# Set your own passcode - this is what unlocks editing on the board
EDIT_PASSCODE=change-me

# These get added automatically when you install the Upstash integration
# from the Vercel Marketplace. If your integration names them differently,
# update lib/redis.ts to match.
KV_REST_API_URL=
KV_REST_API_TOKEN=
